server:
  port: 8084
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/twutter_time_line
    username: postgres
    password: cosu2362
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
  sql:
    init:
      mode: never
  liquibase:
    enabled: false
    change-log: classpath:db/changelog/db.changelog-master.yaml
  application:
    name: TL-SERVICE
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

